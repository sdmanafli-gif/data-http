/**
 * Generic placeholder for a tab — no feature logic yet.
 * All copy in Azerbaijani.
 */
export default function PlaceholderView({ title, description }) {
  return (
    <div className="placeholder-view">
      <h1>{title}</h1>
      <p className="placeholder-view__desc">{description}</p>
    </div>
  )
}
